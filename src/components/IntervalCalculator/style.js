import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    title: {
        textAlign: 'center',
        fontWeight: 'bold',
        fontSize: 24,
        textTransform: 'uppercase',
        color: 'red'
    },
    input: {
        height: 40,
        margin: 12,
        borderWidth: 1,
        padding: 10,
    },
    box: {
        margin: 20,
        borderWidth: 4,
        padding: 10,
    },
});
