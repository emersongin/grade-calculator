import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    title: {
        textAlign: 'center',
        fontWeight: 'bold',
        fontSize: 24,
        textTransform: 'uppercase',
        color: 'red'
    },
    content: {
        padding: 10,
    },
    contentInputs: {
        marginBottom: 40,
    },
    input: {
        height: 40,
        margin: 12,
        borderWidth: 1,
        padding: 10,
    }
});
