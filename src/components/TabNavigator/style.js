import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        flexDirection: 'row',
        height: 50,
        width: '100%',
        backgroundColor: '#ccc',
        justifyContent: 'space-around',
        alignItems: 'center',
    },
    icon: {
        //
    }
});
